#include "main.h"

/**
 * mul - multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to nultiply with the first number
 * Return: The result of multiplication between a param and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
