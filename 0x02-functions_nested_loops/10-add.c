#include "main.h"
/**
 * add - Adds two integers and returns the result
 * @a: First integer
 * @b: Second integer
 * Return: Sum of a and b
 */
int add(int a, int b)
{
	int r;

	r = a + b;

	return (r);
}
